import { createTheme } from "@mui/material";


const theme = createTheme({
    palette:{
        primary:{
            main: '#5932EA',
        },
        secondary:{
            main: '#ffff'
        }
    }
})

export default theme;