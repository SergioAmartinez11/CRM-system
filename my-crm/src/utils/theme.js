import { createTheme } from "@mui/material";


const theme = createTheme({
    palette:{
        primary:{
            contrast: '#F2EFFF',
            main: '#5932EA',
            light: '#fffff'
        },
        secondary:{
            main: '#ffff'
        }
    }
})

export default theme;